1. Поправил логику работы секции Portfolio на адаптиве. Теперь, когда у нас при помощи фильтров, отображается только 2 элемента, они стоят не по центру, как раньше, а слева.
2. Пустой класс на body из за того что в JS мы используем $('#body').toggleClass('fixed');  Если обновить страницу, то пустого класса на body изначально нет. То есть JS удаляет имя класса, но не удаляет само слово class, и получается пустой класс. Наверное, это не проблема?) Или есть решение?

